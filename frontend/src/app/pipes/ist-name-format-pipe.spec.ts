import { IstNameFormatPipe } from './ist-name-format-pipe';

describe('IstNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new IstNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
